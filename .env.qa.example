# Use a dedicated non-production ADS ERP instance and disposable account.
QA_BASE_URL=https://qa-erp.example.com
QA_ADMIN_EMAIL=qa-admin@example.com
QA_ADMIN_PASSWORD=replace-with-a-secret

# Mutation tests remain disabled unless this exact opt-in is supplied.
QA_ALLOW_MUTATIONS=false
