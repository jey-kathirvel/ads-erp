# ADS ERP Android

Android WebView client for `https://erp.ads-ai.in/`.

Build with Android Studio or run `gradle assembleDebug` with Android SDK 36 installed.
The debug APK is written to `app/build/outputs/apk/debug/app-debug.apk`.
