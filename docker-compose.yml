version: "3.9"

services:

  postgres:

    image: postgres:16

    restart: always

    environment:

      POSTGRES_DB: ads_erp_db

      POSTGRES_USER: postgres

      POSTGRES_PASSWORD: postgres

    ports:

      - "5432:5432"

  app:

    build: .

    ports:

      - "8000:8000"

    depends_on:

      - postgres
