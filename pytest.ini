[pytest]
addopts = -m "not e2e"
markers =
    p0: release-blocking P0 quality checks
    e2e: browser tests against a running ADS ERP environment
    mutation: tests that change QA environment data
testpaths = tests
